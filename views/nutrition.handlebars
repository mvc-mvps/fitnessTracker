<div class='wrapper'>
  <div class='page-title'>
    <h2>Nutrition</h2>
  </div>
  <nav>
    <a href='/exercise'><button class='btn' id='exercise'>go to exercise</button></a>
    <a href='/planner'><button class='btn' id='planner'>go to planner</button></a>
  </nav>
  {{! <form class="form foodInput-form"> }}
  <input id='food-input' type='text' placeholder='Enter a food' />
  <button type='submit' id='search-button'>Search</button>
  {{!
    </form> }}
  <p id='food-output'></p>
    <p>
      Nutrition Name:
      {{name}}
    </p>

    <button id="newFoodBtn">Add New Food</button>

    {{> newFood}}
    {{!-- {{#each nutritions as |nutrition|}}
        <div id="nutrition-card">
            <p>{{nutrition.id}}. Name: {{nutrition.nutrition_name}} | Calories: {{nutrition.nutrition_calories}}
            </p>
        </div>
        {{/each}} --}}
</div>
<script src='/js/nutrition.js'></script>